---
interface Props {
  title: string;
  lang?: string;
}

const { title, lang = 'en' } = Astro.props;
---

<!DOCTYPE html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <!-- canonical URL placeholder -->
    <!-- hreflang links placeholder -->
  </head>
  <body>
    <slot />
  </body>
</html>

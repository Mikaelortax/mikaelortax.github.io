---
interface Props {
  lang?: 'sv' | 'en';
}

const { lang = 'sv' } = Astro.props;
const isEn = lang === 'en';
const projectsHref = isEn ? '/en/projects' : '/projekt';
const aboutHref = isEn ? '/en/about' : '/om';
---

<section class="band band--mesh overflow-hidden" aria-labelledby="hero-title">
  <div class="hero-content">
    <p class="section-kicker reveal-block">
      {isEn ? 'HYBRID PROFILE: SEO + WEB + AUTOMATION' : 'HYBRID PROFIL: SEO + WEB + AUTOMATION'}
    </p>

    <h1 id="hero-title" class="reveal-block reveal-delay-1 mx-auto mt-6 max-w-[56rem] text-4xl font-semibold tracking-tight text-white sm:text-5xl md:text-6xl">
      {isEn ? 'I build practical ' : 'Jag bygger praktiska '}
      <span class="hero-accent">{isEn ? 'digital systems' : 'digitala system'}</span>
      {isEn ? ' with SEO, web engineering, and automation.' : ' med SEO, webbutveckling och automation.'}
    </h1>

    <p class="reveal-block reveal-delay-2 mx-auto mt-7 max-w-[46rem] text-lg leading-relaxed text-neutral-300 sm:text-xl">
      {
        isEn
          ? 'This site is a portfolio of how I think, build, and iterate in real projects. Clear structure, measurable outcomes, and long-term maintainability.'
          : 'Det här är en portfolio över hur jag tänker, bygger och förbättrar i riktiga projekt. Tydlig struktur, mätbar effekt och hållbar drift över tid.'
      }
    </p>

    <div class="reveal-block reveal-delay-3 mt-9 flex flex-wrap justify-center gap-3">
      <a
        href={aboutHref}
        class="btn-primary px-5 py-3 text-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200 focus-visible:ring-offset-2 focus-visible:ring-offset-neutral-950"
      >
        {isEn ? 'How I work' : 'Så arbetar jag'}
      </a>
      <a
        href={projectsHref}
        class="btn-secondary px-5 py-3 text-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white/70 focus-visible:ring-offset-2 focus-visible:ring-offset-neutral-950"
      >
        {isEn ? 'View projects' : 'Se projekt'}
      </a>
    </div>

    <ul class="mx-auto mt-9 grid max-w-[56rem] gap-3 text-sm text-neutral-200 sm:grid-cols-2 lg:grid-cols-4" aria-label={isEn ? 'Capabilities chips' : 'Kompetenschips'}>
      <li class="pill-chip reveal-card px-4 py-3">Technical SEO / AEO</li>
      <li class="pill-chip reveal-card px-4 py-3">Astro / React</li>
      <li class="pill-chip reveal-card px-4 py-3">Automation (n8n)</li>
      <li class="pill-chip reveal-card px-4 py-3">Laravel / Wave</li>
    </ul>

    <p class="mx-auto mt-8 max-w-[56rem] text-sm text-neutral-400">
      <span class="font-semibold text-neutral-200">{isEn ? 'Now:' : 'Nu:'}</span>
      {
        isEn
          ? ' I am building an SEO-driven content workflow between CMS, Search Console, and CRM.'
          : ' bygger jag ett SEO-drivet innehållsflöde mellan CMS, Search Console och CRM.'
      }
    </p>
  </div>
</section>
